<section class="vh-100 gradient-custom">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card bg-dark text-white" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">
            <div class="mb-md-5 mt-md-4 pb-5">
            <%=form_with url: session_create_url, method: :post, local:true do |f|%>
              <h2 class="fw-bold mb-2 text-uppercase"> <%= t('.log') %> </h2>
              <p class="text-white-50 mb-5"> <%= t('.inp') %> </p id="logres">
                            <%unless flash[:notice].blank?%>
                <h2 style = 'color:red'><%=flash[:notice]%></h2>
            <%end%>
            <br>
              <div class="form-outline form-white mb-4">
                <%=f.text_field :username, require: true, class: "form-control form-control-lg" %>
                <label class="form-label" for="typeEmailX"> <%= t('.username') %> </label>
              </div>

              <div class="form-outline form-white mb-4">
                <%=f.password_field :password, require: true, class:"form-control form-control-lg" %>
                <label class="form-label" for="typePasswordX"> <%= t('.password') %> </label>
              </div>
                <%=f.submit t('.log'), class: "btn btn-outline-light btn-lg px-5", require: true, id:'loguser'%>
            </div>
            <div>
              <p class="mb-1"> <%= t('.que') %> 
              <%=link_to t('.reg'), new_user_path, class: "text-white-50 fw-bold"%>
              </p>
            </div>
            <div>
            <%= link_to t('.to_main'), main_path, class: "text-white-90 fw-bold"%>
            </div>
            <%end%>

          </div>
        </div>
      </div>
    </div>
  </div>
</section>