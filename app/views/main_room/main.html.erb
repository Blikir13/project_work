<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<div class = "container">

<header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
<a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
<svg class="bi me-2" width="40" height="32">
<use xlink:href="#bootstrap"></use>
</svg>
  <span class="fs-1">Таный Санта</span>
</a>


<ul class="nav nav-pills">
<% if signed_in?%>
<a>
<%=button_to 'Выйти из учетной записи', session_logout_path, class: "btn btn-outline-primary me-3 fs-2"%>
<%= form_with url: :profile, method: :get do |form| %>
  <%= form.submit "Просмотреть профиль", id: 'submit-btn', class: "btn btn-primary fs-2" %>
  </a>
<% end %>
<%else%>
<%=button_to 'Войти', session_login_path, method: :get, class: "btn btn-primary"%>
<%end%>
</ul>
</header>
</div>


<%= form_with url: 'rooms/createroom', method: :get do |form| %>
  <%= form.submit "Создать комнату", id: '1submit-btn', class: "btn btn-danger btn-floating" %>
  <br>
<% end %>
<%= form_with url: 'main_room/join', method: :get do |form| %>
  <%= form.submit "Вступить в комнату", id: '1submit-btn' %>
  <br>
<% end %>






